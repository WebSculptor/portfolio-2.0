type HeadingPropType = {
  title: string;
  subtitle?: string;
};

type HeroTypes = {
  title: string;
  description?: string;
  showPath?: boolean;
};

type DropDownProps = {
  title: string;
  description?: string;
};
